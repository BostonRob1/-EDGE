{
  "name": "edge",
  "private": true,
  "version": "0.1.0",
  "description": "$EDGE — prediction market aggregator (Polymarket + Kalshi)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:og": "node scripts/build-og-gif.mjs"
  },
  "dependencies": {
    "@vercel/og": "^0.6.4"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "gifenc": "^1.0.3",
    "satori": "^0.26.0"
  }
}
