{
  "name": "edge",
  "private": true,
  "version": "0.1.0",
  "description": "$EDGE — prediction market aggregator (Polymarket + Kalshi)",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/og": "^0.6.4"
  }
}
