# Warpcast — Profile (Crypto-Native Voice)

Warpcast is a Farcaster client, so the wire-level profile is the same FID. This pack provides a separate intro/voice tuned for the Warpcast subculture (which skews more crypto-native, more degen-fluent, more frame-savvy than generalist Farcaster clients).

## Username
@edge

## Display name
$EDGE

## Bio — primary (256 char)
$EDGE: prediction-market terminal. Solana. Frame-gated alpha for holders. Whale tape + divergence radar live on /edge. Watch the action. Before you bet. edge-two-psi.vercel.app

## Bio — alt v1 (terse)
The Bloomberg for prediction markets — onchain. /edge channel. $EDGE on Solana.

## Bio — alt v2 (functional)
We index Polymarket + Kalshi + onchain whale flow. Frame-gated drops on every divergence ≥0.15. /edge channel.

## Connected wallet
Solana treasury wallet (verified via Warpcast). Optional: Ethereum/Base address if running Base presence later.

## Power Badge target
Pursue power badge: requires high-quality cast cadence + verified addresses + earned engagement. Avoid spammy posting.
